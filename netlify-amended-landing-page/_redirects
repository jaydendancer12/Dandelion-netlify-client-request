# Persona landing pages are real files now (commercial-bank-general.html, …),
# generated from persona.html by generate-persona-pages.sh and served by
# Cloudflare Pages at their clean URLs (/commercial-bank-general, …).
#
# The old "/clean-path /persona.html 200" rewrites were removed: Cloudflare Pages
# turns a 200-rewrite to a .html target into a 308 redirect to /persona, which
# collapsed all 8 personas onto the generic page.
